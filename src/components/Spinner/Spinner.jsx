import { SpinnerContainer, SpinnerStyled } from './Spinner.styled';

const Spinner = () => {
  return (
    <SpinnerContainer>
      <SpinnerStyled />
    </SpinnerContainer>
  );
};

export default Spinner;
